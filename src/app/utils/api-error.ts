/**
 * @deprecated Kept for backward compatibility.
 * The canonical application error class now lives in `errorHelpers/AppError.ts`
 * (matching the reference architecture). This module simply re-exports it so
 * older imports keep working.
 */
export { default } from "../errorHelpers/AppError.js";
